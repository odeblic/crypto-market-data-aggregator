[project]
name = "cex-subscriber"
version = "0.1.0"
description = "Subscribe to L2 market data for BTCUSDT from CEX crypto exchanges"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "asyncio>=4.0.0",
    "dataclasses>=0.8",
    "dataclasses-json>=0.6.7",
    "websockets>=17.1",
]
